{
  "name": "communitydao",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "create": "graph create communitydao --node https://api.thegraph.com/deploy/",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ communitydao",
    "create-local": "graph create --node http://localhost:8020/ communitydao",
    "remove-local": "graph remove --node http://localhost:8020/ communitydao",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 communitydao"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.26.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
